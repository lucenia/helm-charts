apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skylite.fullname" . }}-test-connection"
  labels:
    {{- include "skylite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skylite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
