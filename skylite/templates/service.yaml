apiVersion: v1
kind: Service
metadata:
  name: {{ include "skylite.fullname" . }}
  labels:
    {{- include "skylite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skylite.selectorLabels" . | nindent 4 }}
